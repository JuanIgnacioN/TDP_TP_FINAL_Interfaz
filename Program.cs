using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;

namespace WindowsFormsApp1
{
    static class Program
    {
        /// <summary>
        /// Punto de entrada principal para la aplicación.
        /// </summary>
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            //Application.Run(new Login());
            //Application.Run(new PantallPrincipal());
            //Application.Run(new Blanqueo_PIN());
            //Application.Run(new PopUp_Error());
            //Application.Run(new PopUp_Exito());            
            // Application.Run(new SaldoCuentaCorriente());
            //Application.Run(new UltimosMovimientos());
            //Application.Run(new PopUp_Sesión());
        }
    }
}
